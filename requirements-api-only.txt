# Ultra-minimal requirements for Render API deployment
# Only essential dependencies for the FastAPI server

# Core dependencies
numpy==1.26.4
pillow==10.4.0
python-dotenv==1.0.1
pydantic==2.7.0
pydantic-settings==2.2.1

# Web API
fastapi==0.110.2
uvicorn==0.29.0
python-multipart==0.0.9
httpx==0.27.0

# Redis (for caching)
redis==5.0.4

# Communication (optional but lightweight)
twilio==9.0.4
slack-sdk==3.27.1

# Monitoring
sentry-sdk==2.0.1